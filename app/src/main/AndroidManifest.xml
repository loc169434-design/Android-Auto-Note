<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Audio Recording -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!-- Internet (Firebase) -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Foreground Service -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"
        tools:ignore="ForegroundServicesPolicy" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />

    <!-- Notifications (API 33+) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Billing -->
    <uses-permission android:name="com.android.vending.BILLING" />

    <!-- Package visibility: needed on API 30+ to detect Gemini app -->
    <queries>
        <package android:name="com.google.android.apps.bard" />
    </queries>

    <application
        android:name=".AutoNoteApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AndroidAutoNote">

        <!-- Main Activity -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.AndroidAutoNote.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Onboarding / Login Activity -->
        <activity
            android:name=".auth.OnboardingActivity"
            android:exported="false"
            android:theme="@style/Theme.AndroidAutoNote" />

        <!-- Recording Activity (Transparent Dialog from Widget) -->
        <activity
            android:name=".ui.recording.RecordingActivity"
            android:exported="true"
            android:excludeFromRecents="true"
            android:taskAffinity=""
            android:theme="@style/Theme.AndroidAutoNote.Transparent" />

        <!-- Voice Recording Foreground Service -->
        <service
            android:name=".service.VoiceRecordingService"
            android:foregroundServiceType="microphone"
            android:exported="false" />

        <!-- FileProvider for sharing .txt files -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- Gemini Launcher Activity (from TripleActionWidget) -->
        <activity
            android:name=".ui.ai.GeminiLaunchActivity"
            android:exported="false"
            android:excludeFromRecents="true"
            android:taskAffinity=""
            android:theme="@style/Theme.AndroidAutoNote.Transparent" />

        <!-- File Viewer Activity (widget File button + HomeScreen Edit button) -->
        <activity
            android:name=".ui.fileviewer.FileViewerActivity"
            android:exported="true"
            android:excludeFromRecents="false"
            android:theme="@style/Theme.AndroidAutoNote" />

        <!-- Widget: Triple Action 1×3 (Micro + Gemini + File) -->
        <receiver
            android:name=".widget.TripleActionWidgetReceiver"
            android:label="@string/widget_triple_action_title"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/triple_action_widget_info" />
        </receiver>

        <!-- Widget placement success callback — fires blinking animation -->
        <receiver
            android:name=".widget.WidgetPlacedReceiver"
            android:exported="true" />

    </application>

</manifest>